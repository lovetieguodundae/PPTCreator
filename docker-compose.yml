services:
  redis:
    image: redis:7.4-alpine
    container_name: deckflow-redis
    ports:
      - "6379:6379"
    volumes:
      - deckflow-redis-data:/data
    command: redis-server --appendonly yes

volumes:
  deckflow-redis-data:

